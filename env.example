ENV=dev
PROJECT_NAME=agentic_rag
PROJECT_VERSION=0.1.0
PROJECT_DESCRIPTION="An agentic retrieval-augmented generation application."
ALLOWED_ORIGINS=*
BASE_PATH=/agentic_rag
OPENAI_API_KEY="dummy-key"
GEMINI_API_KEY=""

# Working directory for uploads, logs, and vector data (default: current directory).
WORKING_DIR=

# Qdrant collection base name (default: agentic_rag_template).
COLLECTION_NAME=

# Hugging Face cache home (default: WORKING_DIR/hf).
HF_HOME=

JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_PROTOCOL=http 

REDIS_HOST=
REDIS_PORT=
REDIS_PROTOCOL=redis 

# RabbitMQ (optional; used for background jobs like async ingestion)
USE_RABBITMQ_INGESTION=false
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/

# If set, overrides HOST/PORT/USERNAME/PASSWORD/VHOST (example: amqp://guest:guest@rabbitmq:5672/)
RABBITMQ_AMQP_URL=
RABBITMQ_INGEST_QUEUE=agentic_rag.ingest